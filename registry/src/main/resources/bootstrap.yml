spring:
  application:
    name: registry
  profiles:
  	active: development
  cloud:
    config:
      uri: http://localhost:8888
      label: dev
      failFast: true
management:
  security:
    enabled: false
