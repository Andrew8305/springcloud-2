spring:
  application:
    name: gateway
  profiles:
  	active: development
  cloud:
    config:
      uri: http://localhost:8888
      label: dev,sit,uat,prd
      failFast: true
management:
  security:
    enabled: false
