spring:
  application:
    name: gateway
  profiles:
  	active: development
  cloud:
    config:
      uri: http://localhost:8888
      failFast: true
management:
  security:
    enabled: false
