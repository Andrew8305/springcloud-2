spring:
  application:
    name: gateway
  profiles:
  	active: development
  cloud:
    config:
      uri: http://config:8888
      label: dev
      failFast: true
management:
  security:
    enabled: false
