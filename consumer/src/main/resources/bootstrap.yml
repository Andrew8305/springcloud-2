spring:
  application:
    name: consumer
  profiles:
  	active: dev
  cloud:
    config:
      uri: http://localhost:8888
      label: dev,sit,uat,prd
      failFast: true
management:
  security:
    enabled: false
