spring:
  application:
    name: zipkin
  profiles:
  	active: development
  profiles:
  	active: dev
    config:
     discovery:
      enabled: false
     uri: http://config:8888
     profile: development
     label: dev
management:
  security:
    enabled: false
  zipkin:
    locator:
      discover:
        enabled: true