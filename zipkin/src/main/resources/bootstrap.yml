spring:
  application:
    name: zipkin-server
  profiles:
  	active: development
  profiles:
  	active: dev
    config:
     discovery:
      enabled: false
     uri: http://config-server:8888
     profile: development
     label: dev
management:
  security:
    enabled: false
  zipkin:
    locator:
      discover:
        enabled: true
server:
